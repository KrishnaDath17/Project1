import "package:flutter/material.dart";

void main() {
  runApp(MaterialApp(
    home: Scaffold(
        appBar: AppBar(
          title: Text('kits cse'),
          backgroundColor: Color.greenAccent[400],
        ),
        body: center()),
  ));
}
